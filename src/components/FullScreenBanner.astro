---
 interface Props{
    image:string,
    class?: string,
    slot?: string
}

const {image, class:className, ...attr} = Astro.props
const URL_main = `${import.meta.env.BASE_URL}/`
// const style = `background-image: url(${URL_main}banners/${image});background-position: center; mask-image: linear-gradient(0deg, rgba(0,0,0,1) 58%, rgba(249,250,249,0) 100%);`
const style = `background-image: url(${URL_main}banners/${image});background-position: center; mask-image: linear-gradient(0deg, rgba(0,0,0,0) 0%, rgba(0,0,0,1) 19%, rgba(0,0,0,1) 58%, rgba(249,250,249,0) 100%);`
---
<div class="w-screen min-h-screen bg-cover" 
style={style}
>

</div>