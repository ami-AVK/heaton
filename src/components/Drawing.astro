---

const {width, height, parameters} = Astro.props;

// console.log(rect)
---
<svg viewBox={`0 0 ${width} ${height}`} xmlns='http://www.w3.org/2000/svg' height="300px">
    <rect x='0' y='0' width={width}' height={height} stroke='currentColor' stroke-width='0.5%' fill="none"/>
</svg>