<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scroll Snap Example</title>
    <style>
        /* Основной контейнер с прокруткой */
        html {
            scroll-snap-type: y mandatory;
            overflow-y: scroll;
            height: 100vh;
        }

        /* Общий стиль для всех блоков */
        .snap-item {
            scroll-snap-align: start;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
        }

        /* Стили для визуального разделения */
        .snap-item:nth-child(odd) {
            background-color: lightblue;
        }

        .snap-item:nth-child(even) {
            background-color: lightcoral;
        }

        /* Стили для элементов списка */
        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        ul li {
            scroll-snap-align: start;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
        }

        ul li:nth-child(odd) {
            background-color: lightgreen;
        }

        ul li:nth-child(even) {
            background-color: lightyellow;
        }
    </style>
</head>
<body>
    <div class="snap-item">Block 1</div>
    <div class="snap-item">Block 2</div>
    <ul>
        <li class="snap-item">List Item 1</li>
        <li class="snap-item">List Item 2</li>
        <li class="snap-item">List Item 3</li>
        <li class="snap-item">List Item 4</li>
    </ul>
    <div class="snap-item">Block 3</div>
</body>
</html>